# CLAUDE.md

This file provides guidance to Claude Code when working in `server/internal/provider/mocklike/`.

## Scope

This package contains a mock-like DNS provider adapter variant.

## Expectations

- Keep this package aligned with the shared provider contract.
- Keep any simplified provider behavior isolated here.
- Do not rely on package-specific shortcuts elsewhere in the backend.
